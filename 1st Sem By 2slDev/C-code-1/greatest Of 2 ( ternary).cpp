#include<stdio.h>
 int main()
 {
 	int a=0, b=0, max=0 ;
    printf(" enter the value of a and b");
    scanf("%d%d", &a,&b);
    max=7
	a>b ? a:b;
    printf(" largest of %d and %d is %d", &a,&b, max);
    
 	return 0;
 }
